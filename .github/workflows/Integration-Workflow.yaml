name: Integration-Calling-Workflow
run-name: ${{ github.actor }} ran pipeline for APIGEE Integration deployment.
on:
  pull_request:
      branches:
        - development
        - staging
        - production
      types:
        - opened
jobs:
  Call_Integration_Reusable_workflow:
    uses: project/apigee-cicd/.github/workflows/Integration-CICD.yml@main
    secrets: inherit